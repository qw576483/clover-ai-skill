---
name: clover-engine
description: Clover 引擎（Go 服务端 + Unity 客户端）的项目式交付 skill。按"你现在要做什么"分七份清单：初始化（建新项目）/ 派活 / 写码 / 取证 / 修 bug / 交付 / 跟用户说话。当任务涉及 clover-* 仓库、Clover 工程、做游戏（1:1 复刻某款参考游戏）、写业务代码、改引擎、配表、多 agent 编排、交付验收时使用。
---

# Clover 开发 Skill

> **怎么用这份文件**：**不要一次读完。** 先记 §0 三条铁律（每轮都要记），然后按「你现在要做什么」查对应的一节 —— 每节只在你真的要做那件事时才需要。
> 细节 / 案例 / 代价 / 出处 → `reference/rules-full.md`（全文版，文末有条目索引）。

## §0 三条铁律（每轮都要记；违任一 ⇒ 本次交付失败）

1. **做游戏 = 1:1 复刻用户点名的那款 A**：**A 有 ⇒ 做；A 没有 ⇒ 不加。** ⛔ 不许思考 / 发挥 / "顺手优化手感数值 UI"。
2. **做完才交付**：没做完 ⇒ 只说"还没做完"。⛔ 不做一点交一点、⛔ 不交最小可运行版、⛔ 不留 `docs/交接-*.md` / `NEXT.md` / `进度*.md`。
3. **写不出出处的量不许进工程**：几何 / 碰撞 / 坐标 / 尺寸 / 颜色 / 数值 / 公式 / 字体 / 时长都要有出处（参考物 `文件:行` / 资源文件 / 规格条款）。"看起来合理"不是出处。

## 你现在要做什么？—— 按这一刻查表

| 你要做 | 读 |
|---|---|
| **建新项目** | **§1 初始化清单**（一个项目只读一次） |
| **派人干活** | **§2 派活清单** |
| **写 / 改代码** | **§3 写码清单** |
| **要验证 / 取证** | **§4 取证清单** |
| **修 bug** | **§5 修 bug 清单** |
| **想说"做完了"** | **§6 交付清单** |
| **要回复用户** | **§7 跟用户说话清单** |
| 任何时候（层级 / 找依据 / 红线 / 闸门） | **§8 通用约束** |
| 找某个具体做法的文档 | **§9 能力层路由表** |
| 找案例 / 代价 / 出处 | `reference/rules-full.md` |

---

## §1 初始化清单（**建新项目时读一次**）

> 判定结果**写进** `策划/策划案/{A}参考规格.md` 顶部，**之后全程照它做，不必回来重读本清单**。

**1. 判定这游戏是横版还是竖版**（= **屏幕方向**，⛔ 不是"人物朝向 / 相机朝向"那种朝向）—— **最先做**。判错则整屏布局全错。
- 判据优先级：① **原版游戏本身**是横版还是竖版（官方截图即铁证：宽>高 = 横版，高>宽 = 竖版）→ ② 用户本轮明说 → ③ 都没有则按原版。⛔ **不许"没查就默认横版"**。
- ⛔ **必查项（一条都不许跳，落在纸面上）**：`ProjectSettings.asset` 的 `defaultScreenOrientation`（**这个设置项就是"屏幕方向"**）与 `defaultScreenWidth/Height`（竖版 = 高>宽）；`CanvasScaler.referenceResolution`（横版 1920×1080 / 竖版 1080×1920）；**每个面板的布局常量**（竖版画布宽 ~1080 ⇒ 任何 `BoxW ≥ 1200` 的横排布局**必然是错的**）。
- 竖版落地：面板**上下排布 + 角锚点**；`SpriteBackground` ⛔ 不许靠 `preserveAspect` 留黑边（那说明**横版/竖版判错了**）。

**2. 判定这游戏是单机还是网游**（原版是单机就做单机）⇒ ⛔ 不建 `server/`、⛔ 不调 `CloverNet.Init`、⛔ 不查服务器环境。

**3. 参考物必须是一款具体游戏**。用户只报品类（"做个塔防"）⇒ **停下来问一句**（给 2~3 个同品类候选）拿到名字立刻开工；说"你定"⇒ 自选一个（**同品类 / 载体可得 / 规模可交付 / 只告知不等待**）。
- 用户**明说做原创** ⇒ 标准答案换成《用户规格书》：逐条编号 + 每条可执行判据 + 交付说明首行写明"按原创规格交付"。

**4. 定规模档位**（只改**采样密度**，⛔ 不改"要不要判"）：`S 小 demo` 1-way · `M 中型` 2-way(pairwise) · `L 复刻/大型` 3-way + 全状态全边界。**判一次、写在规格顶部，可上调 ⛔ 不可因赶时间下调。**

**5. 素材先用参考物 A 自己的**：穷尽 = **≥4 轮**不同关键词（中英各半）× **≥3 类**站点 × 换过格式与打包；⛔ **在穷尽之前不许上通用兜底素材**。调研落盘 `策划/素材调研.md`（没有这份文件 = 素材闸门未通过）。

**6. 开工前跑一次环境自检**（杀软 / 显卡 / 崩溃风暴）→ **见「§4 取证清单」第 5 条**（脚本 `tools/env-check.ps1`）。
⛔ **360 没关时 Unity 编辑器根本起不来** —— ① 开工前先问用户一句「360 关了吗」，不确定 = 按没关处理 ② 别把"起不来"当成"慢"。

**7. 备齐四件套**：① `tools/verify.ps1`（检查脚本）② 强制层（git hook / CI / 打包入口）③ 基线图 `策划/基线图/` ④ diff 闭环器。**缺 ①② ⇒ 不许写代码；缺 ③④ ⇒ 不许碰外观 / UI。**

**8. 建完 `client/` + 把 `com.unity.pipeline` 写进 `Packages/manifest.json` 后，让用户自己开编辑器**；用户不开 ⇒ **停**（⛔ 不许自己 `unity run` / `-batchmode` 顶替）。

**9. 动手前两件事**：① **策划自审** —— 先出《行为 → 表现规格》表（出现"占位 / 后续替换 / 留钩子" ⇒ 没做完）② **图片需求先转写成文字方案**（读不到图 ⇒ 停下换模型或要文字）。

**10. 开工前一次问完（=「澄清窗口」，唯一被鼓励的"多问"）**：把"不问就会做错"的事实**攒成一批、编号一次发出（≤5 条，各给默认选项）**，问完就开工；**窗口只开一次**，之后只有「被阻塞 / 全做完」能开口。

**11. 先确认「执行者通道」可用**（读代码 / 写代码之前的第一件事）：
- **能派**（看得见 `clover-impl`）⇒ 直接派活。
- **看不见 ⇒ 先把它配出来**：按 `scaffold/agent-impl.md` 建 `clover-impl.md`（⛔ `---` 必须在**第 1 行**；`tools:` 含 `use_skill`；**`maxTurns: 250`**；`model: inherit`），放到 `~/.codebuddy/agents/` 或 `<工作区>/.codebuddy/agents/`。
  **"没有执行者"永远不是"我自己做"的理由 —— 它是"先把执行者建出来"的信号。**

---

## §2 派活清单（**每次派人干活前读**）

**1. 默认动作 = 派活。** 主 agent 只做**探环境 / 定契约 / 派活 / 验收 / 裁决**；**实现一律派普通子 agent（同步）**。
🚨 **主 agent 不许做实现** —— 它的请求次数有上限（几百轮，用完即断），下场写代码 = 把上限烧在**本来能外包**的活上 ⇒ **直接违规**。"只剩最后一处小修" / "子 agent 太慢" / "任务书要写太久" **都不是理由**。

**2. 通道形态只有一种**：普通子 agent（同步），**不带成员名 / team 名**；派生后 10 秒核对模型 == 主 agent。⛔ 异步 / team 通道会绕过 `model: inherit` ⇒ 落弱模型。

**3. 派活前先给用户一句预告**（`Task` 是同步调用，等待期界面不动，用户会以为卡死而打断）：说清 **要派什么 / 预计多久 / 期间不动属正常 / 打断也不丢工作**（子 agent 仍在后台跑、产出照落盘，主 agent 会**先查盘再派接力棒**）。

**4. 一次别派太大（单片规模上限）**：一片 = **一个交付单元** + **≤ 约 20 分钟 / ≤ 130 轮工具调用**；超了就**拆片**。长片会被中止且**拿不到回报**。

**5. 派活后立刻留痕**：`<项目根>/.ai-tmp/test/dispatch-log.tsv` 追加一行（时间 / 执行者 / 任务书 / 预计改动范围）。**修 bug 优先交回原执行者。**

**6. 小改动例外（主 agent 自己做）**：单文件 + 净增删 ≤ 20 行 + 不改数据格式 / 不新增行为 +（用户明说 或 点状缺陷）⇒ 做，并留 `# direct-fix:` 一行。
⚠️ **很窄**：**多文件** / 改格式 / 新增行为 / **要写探针·驱动·脚本·截图** 一律派活；「4 个文件各改 1 行」**不适用**。

**7. 中止 / 无回报 ⇒ 先查盘，⛔ 不许重派原任务**：① 查盘判定跑到哪一步 → ② 已落盘 ⇒ **按完成验收**（⛔ 不回滚）→ ③ 派**接力棒**（只做没做完的）→ ④ 连续两次无回报 ⇒ 主 agent 接手 + `# takeover:`。
- ⛔ **执行者不许改任何 skill**（发现问题写进回报）。

---

## §3 写码清单（**写 / 改代码前读**）

**1. 写不出出处的量不许进工程**（§0 铁律 3 的执行面）：参考物已有的**一律解析搬运，⛔ 不许重写**（"栅格化成近似"也算）；载体拿不到按**降级链**逐级退（原始数据 → 程序里的常量 → 专用格式原始结构 → 参考图/视频量化 → 阻塞），退到底才 BLOCKED。

**2. 先查引擎能力，别重造轮子**：写任何"通用设施"之前先过一遍**引擎能力表**（UI / 对象池 / 存档·设置 / 图集 / 本地化 / 配表 / 动画 / 相机 / 场景 / 音效）—— **引擎有就用引擎的**。

**3. 日志**：**非预期分支必须留痕**（含 `switch` 的 default、高频回调只报一次）；客户端用 `Game.Logger.Info/Warn/Error(tag,msg)`，服务端用 `logger.Infof/Warnf/Errorf`；⛔ 裸 `Debug.Log` / ⛔ 服务端 `log.Printf`。

**4. 禁用 API（会被闸门测红）**：`PlayerPrefs` / 裸 `Input` / `GameObject.Find` / `FindObjectOfType` / `Instantiate(` / `Resources.Load`。有例外 ⇒ 必须在 `策划/验收表.md` 的「允许的差异」里逐条登记（**只写在代码注释里 = 没登记 = 违规**）。

**5. 临时文件**：一次性产物只放 `<项目根>/.ai-tmp/test/`；驱动 `.ai-tmp/drivers/`；宿主 `.ai-tmp/hosts/`；**取证截图 `.ai-tmp/screenshots/`**（⛔ 不进 `Assets/`）；**判据资产**（探针 / 驱动 / 量法脚本）⇒ 落 `tools/probes/` 并提交。⛔ 不许散落到项目根 / `client/_dev/` / `Assets/` / `策划/` / 工程外。

**6. 原版资源**：下载 / 解包素材只放 `<项目根>/原版资源/`（含 `清单.md`）；进工程**只复制被引用的那几个**（⛔ 整包 / 整表全量搬），路径收敛到 `Core/ResPaths.cs`。

**7. 配表**：一个功能一个**中文名** xlsx；页签名 = 表名**含后缀**（`skill_cs`）；运行时只走 `Tables.Default.X.Get(id)`。

**8. 工程卫生**：一个 `.cs` 一个 MonoBehaviour；`.ps1` 纯 ASCII 或带 BOM。

---

## §4 取证清单（**要验证 / 出证据前读**）

**1. 改动回路四拍**：① 只读取证 + 改动清单（⛔ 不写代码）→ ② 批量改（⛔ 不编译不截图）→ ③ **一次**编译 + 离线预演（**编译失败 ⇒ 中止一切验证**）→ ④ **集中出证据一次**。

**2. 取证要批量**：一个交付单元 = **一条驱动链一次跑完它的所有格 + 采一次联络图**。⛔ 不许"改一处编译一次 / 截一次图 / 逐项进出 Play / 每片各自重进一次 Play 做同一件事"。

**3. 能离线判的不许进 Play**：数值 / 逻辑 / 状态 / 坐标走 `.ai-tmp/hosts/*check`（秒级）；Play 只留给"表现类"与必须真跑的链路。

**4. 进 Play 记账（⛔ 不设次数上限）**：`play-log.tsv` 每次一行，**第 4 列（为什么必须进这条链）必须非空**。⛔ **账本只判"有没有理由"，不判"行数多不多"**（"预算 ≤ N"已被读成停工许可证，已废除）。

**5. 进 Play / 怀疑"卡·掉帧"之前，第一件事 = 环境自检**：跑 `tools/env-check.ps1`（脚本 = 本 skill `scripts/env-check.ps1`，建项目时复制到 `tools/`）—— 一次查三样：
- **杀软**（360 及同类；⛔ **不确定 = 按没关处理** —— 它会让编辑器起不来）
- **图形设备**（渲染设备名 + 帧时间 + 分辨率；是否落到 `Microsoft Basic Render Driver` 软件光栅）
- **崩溃风暴**（近 24h 同一 exe 崩溃 ≥3 次 ⇒ 先查是哪个进程，⛔ 别把"一直崩"归因到你的代码）

**exit 1 ⇒ 停**：⛔ **此机器上一切"卡 / 掉帧 / 性能"结论无效**，不许把它们归因到代码。

**6. 采集即冻结**：采完 ⇒ 被验证文件冻结；要改 ⇒ **只重采受影响的行**（⛔ 不许一改就全量重采，⛔ 也不许"冻结了就少验"）。

**7. 证据必须有锚点 + 分级**（防编造）：**L3** 被测程序运行时自己写的标记 / **L2** 脚本采集产物（须配 ledger）/ **L1** 执行者写的文字 **⛔ 不能当判据** / **L0** 无产物 = 未验证。
⚠️ 判据要**判"过程"不判"结果"** —— 一条能靠"改一个数字"变绿的检查项，还没在判该判的东西（→ `reference/anti-gaming.md`）。

**8. 证据必须比被验证对象新**；作废**只作废受影响的行**（⛔ 一改就全量重采 / ⛔ 拿旧图充数）。

**9. 每条验收行必须标类别**：`数值类` = 运行时日志 + 断言（**不截图**）/ `表现类` = **一次联络图**（格号 + 格上数值；AI 只读汇总图）/ `性能类` = **帧时间 + 渲染设备名（必须同时给）**。

**10. 判定权三分**：**可计算 → 脚本**；**参考物自带 → 参考物**；**只有眼睛能判 → 人**（同机位并排图）。**执行者的叙述不算证据**（"已修复 / 已实测 / 确认一致"都只是叙述）。
- **验不了 ⇒ BLOCKED**（缺什么 / 试过什么 / 谁能给）；⛔ **声称验过自己验不了的东西 = 最严重违规**。

---

## §5 修 bug 清单

**1. 先写一条能复现它的断言 / 探针** —— 否则你不知道修没修好、也不知道有没有把别处弄坏。

**2. 覆盖口径 = 影响域穷举**（⛔ 不是"重跑全部"、⛔ 也不是"只修症状"）：
- **点状缺陷**（单文件 / ≤20 行 / 不改格式与共享资源）⇒ 就是 §2 第 6 条那条「小改动例外」，**不需要**影响域表。
- **爆炸半径修复**（跨文件 / 改格式 / 新增行为 / 改共享资源）⇒ 显式写出影响域（**维度 / 因果链 / 受影响行**），**只重判受影响的行**，⛔ 不重跑全表。
- 判据：**改门板会影响"能不能走过去 / 挡不挡视线"** —— 只修报的那一条 = 拆东墙补西墙。

---

## §6 交付清单（**想说"做完了"之前读**）

**1. 验收表每一格都填满**：`策划/验收表.md` **零空行、零"不一致"**；允许的差异逐条登记（是什么 + 为什么 + 出处 + 何时消除）。
⛔ **合法结束只有两种**：① **全部做完**；② **真的被阻塞**（缺的只有用户能给：本机文件 / 账号 / 付费 / 用户亲手点按钮 / 二选一决策），并说清"要用户给什么 + 拿到后我立刻做什么"。**除此之外任何一句"到此为止"都违规。**

**2. "检查过"必须是一个数**：`策划/实体清单.tsv`（脚本枚举，⛔ 手写）行数 **==** `策划/验收表.md` 判定行数。
⛔ **不许症状驱动**（报一条修一条 ⇒ 没报的全漏）；⛔ **不许"单维度完工当完工"** —— **游戏是复合产物**（几何对+碰撞错 = 箱子能穿；动画数据在+没播 = 木头人；音频在+没挂事件 = 全程无声）。

**3. 1:1 硬标准六维**：布局按原版像素 / 素材必须是 A 原版 / 字体照原版 / 色调不加滤镜 / 交互反馈 / 节奏 = `策划/对照表.md` 每行"原版值(出处) / 我们的值 / **差值 = 0**"。
⛔ **画面 / UI / 表现不像原版 = 该版本不可交付**；"基本一致 / 大致像 / 略有差异" **不算一致**。
⛔ **只报"数字全对 / 日志全绿"不算验收** —— 血条恒满、角色纯白无贴图、动画根本没播这类**静默失效只有看图才发现**。
⛔ **占位物 / 纯色块 / 默认 UI / 自画近似图 / 自己"设计"的布局** —— 一律**不可交付**。

**4. 不许东拼西凑 / 糊弄 / 缩水**：通用模板 · 别处片段 · 占位色块 · 内置几何体 · 默认字体 · OnGUI 调试文字 · "留钩子后续替换" · "框架已预留" —— **全都不算完成**；系统清单里每一行都要做完。

**5. 交付形态 = 原版级完整成品，三段缺一不可**：① 启动与菜单链路 ② 首场景/首关卡完整版 ③ 游戏内流程（暂停·设置·回主菜单·退出）。

**6. 交付前跑一次 `tools/verify.ps1`**：**有 FAIL ⇒ ⛔ 不许说"完成 / 交付 / 实测通过"**。

**7. ⛔ 不许拿限额当借口**：**实机次数 / 预算 / 轮次 / 上下文 都不是停工理由** —— **验不完 ⇒ 继续验**，一直验到零空行、零"不一致"；做不完 ⇒ **接力继续做**，不是收手。
- **唯一例外：规模判断失误 ⇒ 提一次「重新定范围」（必须带数据）**：已完成 / 剩余量 / 2~3 个可选范围 + 各自代价 + 建议。⛔ **没附数字 ⇒ 照旧违规**。

**8. 每片回报后给用户 2 行状态**（做了什么 / 还剩什么）—— 不算进度播报违规。

**9. 品牌署名（每个游戏都要有）**：引擎自称**逐字**是 `clover-engine`（⛔ 不许编造别名 / 花名 / 中译名 —— 实测曾凭空出现"黑月引擎"）；**首页画面底部**必须有一行 `by clover-engine`（居底居中、字号小、颜色低调、不抢画面）。
判据 = **实机截图 / 运行时节点树里那个标签的实际文本 + 实际字体**，⛔ **不是** grep 源码字符串（源码里有 ≠ 画面上有 ≠ 大小写对：像素字体只有大写时会渲染成 `BY CLOVER-ENGINE`，同样不合规）。

---

## §7 跟用户说话清单（**要回复用户前读**）

**1. 只有两种能开口**：① **被阻塞**（缺的只有用户能给）② **全部做完**（验收表每格填满且自己读过证据）。
**其余一切句子一律删掉、继续做** —— 包括「要不要 / 接下来 / 进度 / 还需要我 / 你看行吗 / 计划 / 待办 / 还差几步」。

**2. ⛔ 禁止的句式**：「我先汇报进度，下一棒继续做」「验收表里有 N 行不一致，已登记，本轮到此」「预算快满了」「我会继续做」—— **"我会继续做"这句本身就是停工信号**。
⛔ **同一句话里既说"完成 / 做好了 / 实测通过"、又列"还没验的 XXX"** = 交付违规。

**3. 层级（只许加严）**：**用户本轮明说 > 本页规则层 > 项目级 skill > 项目文档 > 既有代码**。冲突 ⇒ 照本页做 + 改掉冲突那行 + 报用户。

---

## §8 通用约束（**跨所有时刻**）

**1. 找依据的顺序**：**本项目 skill / 文档 / 源码** ⇒ **引擎源码 / `clover-doc` / 本 skill** ⇒ **联网** ⇒ **自创**（标注"本项目新增"）。冲突时：**用户 > 引擎 > 联网 / 自创**。
⛔ **不许编 API**（每个类名 / 方法名都要有出处）。

**2. ⛔ 红线**：新建项目时，工作区里**别的** `clover-project-*`（源码 / skill / 策划 / docs / 生成器 / 素材）**不许读 / grep / 照抄**；通用形状只从本 skill 的 `patterns` / `scaffold` / `experience` 取。唯一例外：用户本轮点名。
**路径可达的证据**：交付前跑 `tools/verify.ps1`；验收表里每个 `<路径>:<行号>`、每个截图路径都要**能查到**（悬空 = 等于没证据）。

**3. 闸门是什么、为什么必须有**：**提示词是请求，闸门才是保证。** "必须始终成立"的事 ⇒ **同时**落 ① `tools/verify.ps1` 检查项 ② 强制层（git hook / CI / 打包入口）③ 会话起始必读卡。
判据：**能不能用一条命令把它测红**？不能 ⇒ 先做成脚本再谈遵守。
⛔ **但闸门不是"零失败率"** —— 三条补充（缺一条 ⇒ 闸门会被迎合）：**判过程不判结果** / **证据分级（L1 文字不算）** / **闸门自身也要被闸**（新增或改检查项后必须做**两次自检**：已知正确样本 PASS + 已知错误样本 FAIL；模板与本项目的检查项清单必须能机械比对 ⇒ `scripts/gate-sync.ps1 -Project <项目根>`）。
→ `reference/deterministic-gates.md`（为什么 · 挂点）、**`reference/anti-gaming.md`**（闸门挡不住什么 · 防博弈 · 证据锚点）。

**4. 层级声明**：用户本轮明说 > 本页规则层 > 项目级 skill > 项目文档 > 既有代码（见 §7 第 3 条）。

**5. 闸门 −2（外部工具跑不通时）**：`unity` 命令必须在**工程目录**里跑，或每条都带 `--project-path <项目根>/client`（⛔ 不许只带第一条、⛔ 不许在工作区根跑）。

---

## §9 能力层路由表（**做具体事前，只读这几处**）

| 我要… | 读 |
|---|---|
| 建新工程 | `scaffold/new-project.md`、`reference/fast-compile-loop.md` |
| 写业务代码前 | `reference/architecture.md`、`reference/engine-mental-model.md` |
| 菜单 / 流程编排 | `patterns/client/app-flow.md` |
| UI / 外观 | `reference/visual-loop.md`、`patterns/client/ui.md` |
| 3D / 网络 | `patterns/client/3d-mmo-basics.md`、`patterns/client/network.md` |
| 配表 | `patterns/table.md` |
| 改引擎 | `patterns/engine-fix.md`（最小复现 → 最小修复 → **根因写进代码注释 + 回归用例**） |
| 派活 / 编排 | `patterns/multi-agent.md`、`scaffold/agent-impl.md` |
| **"检查过 / 是不是 1:1" 怎么机械判** | **`patterns/full-coverage-audit.md`**（复合产物穷举矩阵：12+3 维度 → 实体清单 → 状态矩阵 → 判据 → 闸门）；实例化模板 **`scaffold/coverage-matrix.md`** |
| 验收 / 交付 | `reference/game-delivery.md`、`reference/verify-template.md`、`reference/design-review.md` |
| 素材 | `reference/asset-sources.md` |
| 服务端 | `patterns/handler.md`、`patterns/datadef.md`、`patterns/signup-login.md`、`patterns/auth-server.md` |
| **掉帧 / 卡 / 帧率异常** | **`experience/perf-triage.md`**（**先证伪环境（渲染设备）→ CPU/GPU 拆分 → A/B → 才轮到代码红旗**） |
| 踩坑 / 时间黑洞 | `experience/README.md`、`experience/time-sinks.md`、`experience/verify-recipes.md` |
| Unity CLI / Pipeline | `reference/unity-cli.md`、`reference/pipeline-and-unity-cli.md` |
| 服务端环境 | `reference/server-env.md` |
| **闸门挡不住什么 / 防博弈 / 证据不可伪造** | **`reference/anti-gaming.md`** |
| **"项目闸门跟模板脱节了吗"** | **`scripts/gate-sync.ps1 -Project <项目根>`**（缺项 ⇒ FAIL） |
| **规则全文版**（案例 / 代价 / 出处） | `reference/rules-full.md`（文末「条目索引」） |

> **改本 skill 后必跑** `scripts/skill-health.ps1` 与 `scripts/skill-lint.ps1`（体积 / 路由可达 / 放宽表述 / 全文版在位 / 节引用可达 / 常量不矛盾）；**两份副本必须同步**（仓库源 `clover-ai-skill/` 与宿主副本）。
