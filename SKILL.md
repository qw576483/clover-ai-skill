---
name: clover-engine
description: Clover 引擎（Go 服务端 + Unity 客户端）的项目式交付 skill：做游戏/demo（1:1 复刻某款参考游戏）、写业务代码、改引擎、配表、多 agent 编排、交付验收。含规则层（三条铁律 + 成本闸门 + 路由表）。当任务涉及 clover-* 仓库、Clover 工程、做游戏、Unity 客户端或 Go 服务端开发时使用。
---

# Clover 开发 Skill · 规则层（精简入口）

> **入口（本页）= 祈使句 + 成本闸门 + 路由表 + 指针，供"每轮必读"**；**全文版 = `reference/rules-full.md`**（同一套规则 + 案例 + 代价数字 + 出处，**一行未删**，文末有「条目索引」）。
> **入口 ↔ 全文版**：§0 → §0/§0.5 · **§0.1 → §1/§2** · §0.5 → §0.6 · §1 → §5 · §2 → §1.13(T0) · §3 → §1.13 · §4 → §1.12 · §5 → §1.5/§1.11/§2 · §6 → §4 · §7 → §6/§1.10 · §8 → §1.6~§1.9/§7。冲突时以本页祈使句为准（更硬），差异报用户。
> **改本 skill 后必跑** `scripts/skill-health.ps1 -RepoRoot <仓库副本>`（体积 / 路由可达 / 放宽表述 / 两副本一致 / **全文版在位**）。两份副本必须同步（仓库源 `clover-ai-skill/` 与宿主副本）。

## T0 ⛔ 穷举是硬约束（优先级高于三条铁律）

> **交付前必须证明**：**全维度 × 全状态 × 全边界值** 都判过 —— **漏一行 = 交付失败**。
> **执行口径（"检查过"是一个数）**：`策划/实体清单.tsv`（脚本枚举，⛔ 手写）行数 == `策划/验收表.md` 判定行数，**零空行、零 `不一致`**；12+3 维度每维都有行。
> **规模档位**（只改**采样密度**，⛔ 不改"要不要判"）：`S 小 demo` 1-way · `M 中型` 2-way(pairwise) · `L 复刻/大型` 3-way + 全状态全边界。判一次、写在规格顶部，可上调 ⛔ 不可因赶时间下调。
> **⛔ 不许症状驱动**（报一条修一条 ⇒ 没报的全漏）；⛔ 不许"单维度完工当完工"（**游戏是复合产物**，验收对象是合成结果）。
> **依据（CDV · TRC/TCR · NIST 组合测试 · MBT）/ 维度表 / 档位 / 闸门 / 修 bug 影响域口径 → `patterns/full-coverage-audit.md`**；实例化模板 → `scaffold/coverage-matrix.md`。

---

## §0 三条铁律（违任一 ⇒ 本次交付失败）

1. **做游戏 = 1:1 复刻用户点名的那款 A**：A 有 ⇒ 做；A 没有 ⇒ 不加。⛔ 不许思考 / 发挥 / "顺手优化手感数值 UI"。
   - 没给游戏名 ⇒ **先问一句**（2~3 个同品类标杆候选）。说"你定" ⇒ 自选一个（**同品类 / 载体可得 / 规模可交付 / 只告知不等待**），写进 `策划/策划案/{A}参考规格.md` 顶部「形态」后立刻开工。
   - **明说做原创** ⇒ 标准答案换成《用户规格书》：逐条编号 + 每条可执行判据 + 交付说明首行写明"按原创规格交付"。
2. **做完才交付**：⛔ 不做一点交一点、⛔ 不交最小可运行版、⛔ 不留 `docs/交接-*.md` / `NEXT.md` / `进度*.md`、⛔ 不同一条里既说"完成"又列"还没验的"、⛔ 不问"要不要继续 / 还需要我做什么"（那不是被阻塞）。
   没做完 ⇒ 只说"还没做完"。**接力 = 主 agent 立刻开下一个执行者**，不是留 md 给未来。
   ⛔ **主 agent 的请求次数有上限** ⇒ 它**只派活 / 验收 / 裁决，实现一律派执行者**（→ §1 的三条硬强调）；"我自己来做"是违规。
3. **写不出出处的量不许进工程**：几何 / 碰撞 / 坐标 / 尺寸 / 颜色 / 数值 / 公式 / 字体 / 时长都要有出处（参考物 `文件:行` / 资源文件 / 规格条款）。"看起来合理"不是出处；参考物已有的一律**解析搬运**，⛔ 不许重写（"栅格化成近似"也算）。载体拿不到按**降级链**逐级退（→ §7 指针），退到底才 BLOCKED。

## §0.1 ⛔ 两件绝不打折的事（**优先级高于一切"省时间 / 省预算"的考虑**）

> 出处：用户 2026-09-20 原话 —— 「**现在一句话根本出不来游戏，ai一直在偷懒，一直在东拼西凑，一直在糊弄人！！！！！！**」
> 「**去掉 play 次数的约束**，现在约束完，ai 更偷懒了」「**强调必须做完的重要性，强调画面 ui 和画面表现原版的重要性！！！**」
> 病根：规则里 `实机次数 / 预算 / 轮次 / 上下文` 这类**限额**被读成了**停工的许可证** ——
> AI 会**验到限额就收手，剩下没验的直接跳过，然后宣布"完成"**。
> ⇒ **从本节起，这类限额一律废除**：它们是"怎么做得更省"，**不是"可以不做"**。

### ① 必须做完 —— 没有任何"次数 / 预算 / 轮次 / 上下文"能当停下来的理由

- ⛔ **不许东拼西凑**：拿通用模板 / 别处片段 / 自创设施拼出一个"看起来能跑"的壳。A 有的按 A 做，A 没有的不加（铁律 1 / 3）。
- ⛔ **不许糊弄**：占位色块 / 内置几何体 / 默认字体 / OnGUI 调试文字 / "留钩子后续替换" / "框架已预留" **全都不算完成**。
- ⛔ **不许缩水**：系统清单里每一行都要做完。"先做精简版 / 这轮先到这儿 / 下一棒接着做 / 先汇报进度" = **没做完**。
- ⛔ **游戏是复合产物**：验收对象是**各维度合成后的样子**，不是任一维度 —— **单维度做完 ≠ 做完**（几何对 + 碰撞错 = 箱子能穿；UI 元件对 + 状态错 = 没反馈；动画数据在 + 没播 = 木头人；音频文件在 + 没挂事件 = 全程无声）。按维度穷举口径 → `patterns/full-coverage-audit.md`。
- ⛔ **不许拿限额当借口**：**实机次数 / 预算 / 轮次 / 上下文 / token 都不是停工理由**。
  **验不完 ⇒ 继续验，一直验到验收表每一格都填满、零空行、零"不一致"。** 做不完 ⇒ **接力继续做**，不是收手。
- **合法结束只有两种**：① **全部做完**；② **真的被阻塞**（缺的只有用户能给：本机文件 / 账号 / 付费 / 用户亲手点按钮 / 二选一决策），
  并说清"要用户给什么 + 拿到后我立刻做什么"。**除此之外任何一句"到此为止"都违规。**
- **例外之一：规模判断失误 ⇒ 提「重新定范围」（必须带数据）**。当**当前目标在合理成本内不可能做完**时，允许**提一次**：给出「已完成 / 剩余量 / 2~3 个可选范围 + 各自代价 + 建议」，由用户裁一次。
  ⛔ 它与"不许问要不要继续"**不冲突**：问"要不要继续做"= 违规；提"目标需要改范围"= 合法。
  ⛔ 不许拿它当收手通道：**没附剩余量与代价数字 ⇒ 照旧违规**（≤20 字的空口"做不完"不算）。

### ② 画面 / UI / 画面表现必须和原版一个样 —— "数字对"永远不等于"做完了"

- **朝向（横版 / 竖版）是这一条的第 0 项** —— 朝向判错，整屏布局**全是错的**（用户的判词：「竖版游戏，你用横板ui，真有你的」）。
  先按 `§6 闸门 0` 判一次、写进规格顶部，再谈任何布局；⛔ **不许"没查就默认横屏"**（`patterns/client/ui.md` 里那条"默认 1920×1080 横屏"的旧默认已废除）。
- 用户点名 A ⇒ **默认 1:1 复刻**（**不是"像"**）：**朝向 / 布局 / 素材 / 字体 / 配色 / 尺寸 / 层级 / 间距 / 交互反馈 / 节奏**逐项对齐 A 的原版画面。
- ⛔ **占位物 / 纯色块 / 默认 UI / 自画近似图 / 自己"设计"一版顺眼的布局** —— 一律**不可交付**。
  **原版素材就在手上却用纯色块拼 UI** = 最典型的一种"东拼西凑"：先按 `reference/asset-sources.md` 把 A 的 UI 素材接进来（先出**素材索引**：帧号 ↔ 尺寸 ↔ 对应哪个控件），再接面板。
- ⛔ **只报"数字全对 / 日志全绿"不算验收**：血条恒满、角色纯白无贴图、UI 小到看不清、动画根本没播 ——
  这类**静默失效只有看图才发现**。`表现类` 必须进**联络图**并逐格读结论（`reference/design-review.md` §3.4 / §3.6）。
- **判据**：交付前把成品与 A 的原版画面**同机位并排**逐项比 → 每项只允许写 `一致` / `不一致（差在哪）`；
  **出现任何一项 `不一致` ⇒ 该版本不可交付**，改完重截再比，**循环到全部一致**。
- ⛔ **不允许"基本一致 / 大致像 / 略有差异 / 后续可优化"** —— 那只是"没做完"的另一种说法。

## §0.5 元规则：必然性规则必须做成闸门（**提示词是请求，闸门才是保证**）

"必须始终成立"的事（不许编数值 / 交付必过闸门 / 编译失败必停 / 外观必对照）⇒ **同时**落 ① `tools/verify.ps1` 检查项 ② 强制层（git hook / CI / 打包入口）③ 会话起始必读卡。
判据：**能不能用一条命令把它测红**？不能 ⇒ 先做成脚本再谈遵守。细节 → `reference/deterministic-gates.md`

## §1 谁来做

> ⛔ **默认动作 = 派活。** 本节其余内容都是这条的注解。
> 除下面那条**很窄的**「小改动例外」以外，**一切实现都派给普通子 agent（同步）—— 不是 team / 不是异步成员**。
> 主 agent 自己下场写代码 = 违规：它那几百轮该花在**派活 / 验收 / 裁决**上，下场只会留下一堆半成品。
> 出处：用户 2026-09-20 原话 —— 「**你干啥呢？skill 不是说要把大部分任务交给子 agent（不是 team）去做吗？**」

> 🚨🚨 **主 agent 不许做实现（硬强调，连说三遍）** —— 因为 **主 agent 的请求次数是有上限的**（几百轮，用完即断），
> 每一轮都必须花在**派活 / 验收 / 裁决**上；下场写代码 = 把上限烧在**本来能外包**的活上 ⇒ **直接违规**。
>
> 1. **不许主 agent 自己做实现** —— 读代码 / 定方案 / 写文件 / 跑编译 / 跑探针 / 截图，**全部派给执行者**（`clover-impl`）。
> 2. **不许主 agent 自己做实现** —— 哪怕"我看一眼就知道怎么改"，也要把它写成**任务书**派出去；
>    主 agent 的产出是**任务书与判定**，不是键盘上的改动。
> 3. **不许主 agent 自己做实现** —— "只剩最后一处小修"、"子 agent 太慢"、"任务书要写太久" **都不是理由**：
>    派活是**默认动作**，自己动手才是需要写理由的**例外**（且只限 §1「小改动例外」那条窄口径）。
>
> 出处：用户 2026-09-20 原话 —— 「**再次在 skill 里强调子 agent 重要性！！！不要主 agent 处理，主 agent 有请求次数限制啊！！！**」

| 角色 | 做什么 |
|---|---|
| **主 agent** | 探环境 / 定契约 / 派活 / 验收 / 裁决。**不做实现**（请求次数有限，烧不起） |
| **执行者**（普通子 agent） | 干到底；⛔ 不许再派子 agent；⛔ 不许改任何 skill（问题写进回报） |

### 闸门 −1：**先确认「执行者通道」可用，再谈开工**（最该机械执行的一条，30 秒的事）

开工第一件事（**在读代码、写代码之前**）：确认任务工具里**能派生普通子 agent**。

1. **能派**（工具里看得见执行者，例如 `clover-impl`）⇒ 直接按第 3 步派活。
2. **看不见 ⇒ 环境没配，先把它配出来，⛔ 不许因此改成"自己做"**：
   - 按 `scaffold/agent-impl.md` 建 `clover-impl.md`（内容 = 该文件**从第一行 `---` 起的全部内容**），放到
     `~/.codebuddy/agents/clover-impl.md`（用户级，推荐）或 `<工作区>/.codebuddy/agents/clover-impl.md`（项目级）；
   - ⛔ `---` 必须在**文件第 1 行**（前面多一行标题 / 说明 ⇒ `name` / `tools` / `maxTurns` **全部静默失效**）；
   - ⛔ `tools:` 必须显式含 `use_skill`；`maxTurns: 450`（压在本机 ~500 上限之下）；`model: inherit`；
   - 放好后**下一条消息即可 spawn，不必重启**（`/agents` 可确认是否已加载）。
3. **⛔ 反例（2026-09-20 实测事故，本节就是它的存在理由）**：任务工具里只列出了内置的**只读** `code-explorer`，
   主 agent 于是判"没有可派活的通道"，**自己上手改了 4 个源文件 + 1 个探针 + 1 个驱动**，被用户当场揪出来。
   **正确动作**：通道缺失 ⇒ **按第 2 条把 `clover-impl.md` 建出来**（模板现成，30 秒），**然后派活**。
   **"没有执行者"永远不是"我自己做"的理由 —— 它是"先把执行者建出来"的信号。**

- **通道形态只有一种**：**普通子 agent（同步）** —— 给 agent 名 + 自包含任务书，**不带成员名 / team 名**。
  ⛔ 不许异步成员 / team 通道（绕过 `model: inherit` ⇒ 落弱模型）；派生后 10 秒核对模型 = 主 agent。
- **唯一例外（主 agent 自己做）**：单文件 + 净增删 ≤ 20 行 + 不改数据格式 / 不新增行为 +（用户明说 或 点状缺陷）⇒ 做，并在 `.ai-tmp/test/dispatch-log.tsv` 留 `# direct-fix:` 一行。
  ⚠️ **这条例外很窄，别把它读宽**：**多文件** / 改了数据格式 / 新增了行为 / **要写探针·驱动·脚本·截图**的 —— 一律派活，哪怕只多一个文件。
  「4 个文件各改 1 行」**不适用**本例外（那不是"单文件"）；"改动小"也不是理由（判据是文件数 + 行数 + 行为，不是"我觉得简单"）。
- 派活后立刻留痕（`dispatch-log.tsv` 一行，对账用）；**修 bug 优先交回原执行者**。
- ⛔ **单片规模上限（防"等待期被打断"）**：一片 = **一个交付单元**（能被一条命令判生死的那一级）+ **≤ 约 20 分钟 / ≤ 130 轮工具调用**；超了就**拆片**，⛔ 不许"一片干到底"。
  为什么：宿主对**单个子任务的等待**有上限 —— 实测长片（30~70 分钟）会被中止，报 `code=10003 This operation was aborted` / `No result found`，
  而**子 agent 往往仍在后台跑、产出照样落盘，调用方却拿不到回报** ⇒ 无法验收，只能错位接力（2026-09-20/21 实测三次）。
  配置依据：`scaffold/agent-impl.md` 的 **`maxTurns: 250`**（≈ 单片 15~30 分钟）+ `patterns/multi-agent.md` 第 1 步拆片。
- **⛔ 中止 / 无回报的标准处置（⛔ 不许重派原任务）**：① **先查盘**（`git status` + 近 1~3 小时 mtime + `.ai-tmp/test/` 最新产物）判定"它跑到哪一步"；
  ② 已落盘 ⇒ **按完成验收**（产出算数，⛔ 不回滚）；③ 派**接力棒** —— 任务书里把「上一棒做到哪 / 还剩什么」写进"前置依赖"一节，让它**只做没做完的**；
  ④ 连续两次无回报 ⇒ 主 agent 接手并在 `dispatch-log.tsv` 记 `# takeover:`。
  实测形状：首棒改完 `.bytes`/几何后被打断，接力棒**只补场景重生成 + 端到端验证**就收口（不重做已完成的）。
- 细节 → `patterns/multi-agent.md`、`scaffold/agent-impl.md`
- **修 bug 的覆盖口径**：**点状缺陷**（单文件 / ≤20 行 / 不改格式与共享资源）⇒ 就是上面那条「小改动例外」，**不需要**影响域表；**爆炸半径修复**（跨文件 / 改格式 / 新增行为 / 改共享资源）⇒ 影响域穷举（因果链可达的子集，**不重跑全表**，只重判受影响行）→ `patterns/full-coverage-audit.md` §9。⛔ 只修症状 = 拆东墙补西墙。
- 细节 → `patterns/multi-agent.md`、`scaffold/agent-impl.md`

## §2 成本闸门（**管"别重复劳动"，不管"能验几次"**）

> ⚠️ **先把这条读对**：本节**唯一**要管的是**同一件事被重复做几遍**（效率）。
> ⛔ **它绝不限制"总共能验几次"** —— **没有次数上限、没有预算、没有"用尽即停"**（§0.1 ①）。
> "次数 / 预算 / 上下文"**从来不是停工理由**；验不完就继续验。

1. **取证要批量（但≠只许一次）**：一个交付单元（= 能被一条命令判生死的那一级）**用一条驱动链一次跑完它的所有格 + 采一次联络图**。
   ⛔ 不许"改一处编译一次 / 改一处截一次图 / 逐项进出 Play"（把分钟级动作重复 N 次）；
   ⛔ 不许"每片各自重进一次 Play 做同一件事"。**需要再进**（新批次 / 诊断态 / 新的因果范围）就**照常进**，写得出理由即可。
2. **进 Play 记账，但不设上限**：每进一次往 `<项目根>/.ai-tmp/test/play-log.tsv` 追加一行
   `时间 / 执行者 / 片名 / 为什么必须进这条链`；**第 4 列必须非空**（写不出理由 ⇒ 说明本可以用离线断言）。
   ⛔ **没有"预算 ≤ N"、没有"用尽就停"** —— 有理由就继续，**一直验到验收表填满为止**。
3. **驱动先复用再新建**：`.ai-tmp/drivers/` = **本轮复用**（交付前统一清）；`.ai-tmp/test/` = 真一次性（用完即删）。
4. **采集即冻结**：采完 ⇒ 被验证文件冻结；要改 ⇒ 只重采受影响的行（⛔ 不许一改就全量重采，⛔ 也不许"冻结了就少验"）。
5. **能离线判的不许进 Play**：数值 / 逻辑 / 状态 / 坐标走 `.ai-tmp/hosts/*check`（秒级）；Play 只留给"表现类"与必须真跑的链路。
6. 闸门：`verify.ps1` 的 `play-ledger` / `freeze-before-capture` / `evidence-economy`（模板第 16~18 项）。
   ⛔ **账本只判"有没有理由"，不判"行数多不多"** —— 拿行数判超支的检查已废除。
7. **进 Play 的第一件事 = 环境基线**（渲染设备名 + 帧时间 + 分辨率），⛔ 不许在确认渲染设备之前把"卡 / 掉帧"归因到代码。**2026-09-20 实测：跑满 6 次 Play 才去读 `SystemInfo.graphicsDeviceName`，而根因就在这一条里**（机器在用 WARP 软件渲染，3.5 fps）→ `experience/perf-triage.md`。
- 细节 → `reference/verify-template.md`、`reference/visual-loop.md`、`reference/fast-compile-loop.md`

## §3 改动回路（四拍，别跳）

① 只读取证 + 改动清单（⛔ 不写代码）→ ② 批量改（⛔ 不编译不截图）→ ③ **一次**编译 + 离线预演（**编译失败 ⇒ 中止一切验证**）→ ④ **集中出证据一次**。
- 便宜的多跑，贵的攒一批只做一次；修 bug：先写能复现的断言 → 只跑那条路径 → 只重采它那行。
- **动手前四件套**：① `tools/verify.ps1` ② 强制层 ③ 基线图 `策划/基线图/` ④ diff 闭环器。**缺 ①② ⇒ 不许写代码；缺 ③④ ⇒ 不许碰外观 / UI**。
- **交付形态 = 原版级完整成品，三段缺一不可**：① 启动与菜单链路 ② 首场景/首关卡完整版 ③ 游戏内流程（暂停·设置·回主菜单·退出）。
  且**画面 / UI / 表现必须 1:1 原版**（§0.1 ②）—— **"能跑"只是及格线，"长得像原版"才是交付线**。
- 细节 → `reference/change-loop.md`、`reference/deterministic-gates.md`、`reference/visual-loop.md`、`reference/game-delivery.md`

## §4 证据契约（判定权不在执行者手里）

- 判定权三分：**可计算 → 脚本**；**参考物自带 → 参考物**；**只有眼睛能判 → 人**（同机位并排图）。执行者的**叙述不算证据**。
- 类别：`数值类` = 运行时日志 + 断言（**不截图**）；`表现类` = **一次联络图**（格号 + 格上数值；AI 只读汇总图）；**`性能类` = 帧时间 + 渲染设备名（必须同时给：设备是 `Microsoft Basic Render Driver` 时数字无意义）**。
- 验不了 ⇒ **BLOCKED**（缺什么 / 试过什么 / 谁能给）；⛔ **声称验过自己验不了的东西 = 最严重违规**。
- 证据必须**比被验证对象新**；作废**只作废受影响的行**（⛔ 一改就全量重采 / ⛔ 拿旧图充数）。
- 细节 → `reference/game-delivery.md`、`reference/visual-loop.md`

## §5 交付闸门

- `策划/验收表.md`：每格填满 + 每行标 `数值类`/`表现类` + **零"不一致"**；允许的差异逐条登记（是什么 + 为什么 + 出处 + 何时消除）。
- **1:1 硬标准六维**（布局按原版像素 / 素材必须 A 原版 / 字体照原版 / 色调不加滤镜 / 交互反馈 / 节奏）= `策划/对照表.md` 每行"原版值(出处) / 我们的值 / **差值 = 0**"。
  ⛔ **画面 / UI / 表现不像原版 = 该版本不可交付**；"基本一致 / 大致像 / 略有差异" **不算一致**（§0.1 ②）。
- 交付前跑一次 `tools/verify.ps1`（**含 §1.11 的 8 条机械自检**）：**有 FAIL ⇒ ⛔ 不许说"完成 / 交付 / 实测通过"**。
- ⛔ **"检查过"必须是一个数**：`策划/实体清单.tsv` 行数 == `策划/验收表.md` 判定行数、**零空行、零 `不一致`**（口径与三条闸门 → `patterns/full-coverage-audit.md`）。
  ⛔ **不许症状驱动**：只修用户报的那一条 ⇒ 没报的全漏（治法 = 上表 + 闸门，不是"我再仔细看看"）。
- ⛔ **合法结束只有两种**：① 全做完；② **真被阻塞**（缺的只有用户能给：本机文件 / 账号 / 付费 / 用户点按钮 / 二选一决策），且说清"要用户给什么 + 拿到后我立刻做什么"。
  ⛔ 禁止用这些当结束理由：**预算快满 / 下一棒接着做 / "不一致已登记本轮到此" / "先汇报进度"**（登记 ≠ 交付）。
- 每片回报后**主 agent 给用户 2 行状态**（做了什么 / 还剩什么）—— 不算进度播报违规。
- 细节 → `reference/game-delivery.md`、`reference/verify-template.md`、`reference/design-review.md`

## §6 五道前置闸门（不通过就停）

**闸门 0（最先做）：判定朝向 —— 横版 / 竖版**。出处：用户 2026-09-20 原话 ——「**竖版游戏，你用横板ui，真有你的**」。
- **判据优先级**：① 原版这款游戏的**原生朝向**（联网搜一轮 `<游戏名> 横版还是竖版` / `<game> portrait or landscape`；A 的**官方截图本身即铁证**——宽>高=横版，高>宽=竖版）→ ② 用户本轮是否明说 → ③ 都没有则按原版。⛔ 不许"没查就默认横版"。
- ⛔ **必查项（一条都不许跳，落在纸面上）**：`ProjectSettings.asset` 的 `defaultScreenOrientation`（`Portrait=1` / `LandscapeRight=4` …）与 `defaultScreenWidth/Height`（**竖版 = 高 > 宽**，如 1080×1920）；引擎 `UIManager` 的 `CanvasScaler.referenceResolution`（横版 `1920×1080` / 竖版 `1080×1920`，见 `Runtime/Presentation/CloverPresentation.cs:85` 的 `ReferenceResolution`（默认 1920×1080，**可配置**），竖版项目在业务侧设成 1080×1920 即可）；**每个面板的布局常量**（竖版画布宽只有 ~1080 ⇒ 任何 `BoxW ≥ 1200` 的横排布局**必然是错的**）。
- **竖版落地口径**：面板一律**上下排布 + 角锚点**（⛔ 左右并排的"标签–输入框–按钮"一行要改成三行）；`SpriteBackground` ⛔ 不许靠 `preserveAspect` 留左右黑边（原版竖构图本来就该铺满竖屏，遇到"原图是竖的、画布是横的"就是**朝向判错了**，不是"用 preserveAspect 兜住"）。
- **判定结果写进** `策划/策划案/{A}参考规格.md` 顶部「形态」一节（与形态同一处），**只判一次**，全程照做、不许反复自检。
- **判据**：能一句话回答「横/竖 + 画布参考分辨率 + 哪些面板按竖版重排」，且成品截图与 A 官方截图**同机位并排逐项 `一致`**（→ §0.1 ② / §5）。

① **判形态**（单机 / 网游 / 单机+联网）：原版是单机就做单机 ⇒ ⛔ 不建 `server/`、⛔ 不调 `CloverNet.Init`、⛔ 不查服务器环境；判一次、写进规格顶部、全程照做。
② **环境自检（跑一条命令，别靠记）**：`powershell -NoProfile -ExecutionPolicy Bypass -File tools/env-check.ps1` —— 一次查两样：**杀软**（360 及同类；不确定 = 按没关处理）与**图形设备**（物理卡是否 `Code 22/10/43`、Unity 是否落到 `Microsoft Basic Render Driver` 软光栅）。**exit 1 ⇒ 停**，先修环境（修法脚本末尾自己打），⛔ 此机器上一切"卡 / 掉帧 / 性能"结论**无效**。脚本 = 本 skill `scripts/env-check.ps1`，建项目时复制到 `tools/`。
③ 建完 `client/` + 把 `com.unity.pipeline` 写进 `Packages/manifest.json` 后，**让用户自己开编辑器**；用户不开 ⇒ **停**（⛔ 不许自己 `unity run` / `-batchmode` 顶替）。
④ **素材先用参考物自己的**：穷尽 = ≥4 轮关键词（中英各半）× ≥3 类站点 × 换过格式与打包；穷尽前 ⛔ 不许上通用兜底素材。
- **闸门 2b**：每条 `unity` 命令都要在**工程目录**里跑，或每条都带 `--project-path <项目根>/client`（⛔ 不许只带第一条、⛔ 不许在工作区根跑）；`unity status` 当"编辑器活着"的证据时必须同时给工程路径。
- **动手前两件事**：① 策划自审（《行为 → 表现规格》表；出现"占位 / 后续替换 / 留钩子"⇒ 没做完）② 图片需求先转写成文字方案（读不到图 ⇒ 停下换模型或要文字）。
- 细节 → `patterns/game-demo.md`（§1.5 / §4.0）、`reference/asset-sources.md`、`reference/game-delivery.md`、`reference/pipeline-and-unity-cli.md`

## §7 找依据与层级

- 顺序：**本项目 skill/文档/源码** ⇒ **引擎源码 / `clover-doc` / 本 skill** ⇒ **联网** ⇒ **自创**（标注"本项目新增"）。冲突：**用户 > 引擎 > 联网 / 自创**。⛔ 不许编 API（每个类名 / 方法名都要有出处）。
- ⛔ **红线**：新建项目时，工作区里**别的** `clover-project-*`（源码 / skill / 策划 / docs / 生成器 / 素材）**不许读 / grep / 照抄**；通用形状只从本 skill 的 `patterns`/`scaffold`/`experience` 取。唯一例外：用户本轮点名。
- **层级（只许加严）**：用户本轮明说 > 本页规则层 > 项目级 skill > 项目文档 > 既有代码。冲突 ⇒ 照本页做 + 改掉冲突那行 + 报用户。
- **澄清窗口**（唯一被鼓励的"多问"）：把"不问就会做错"的事实**攒成一批、编号一次发出（≤5 条，各给默认选项）**，问完就开工；**窗口只开一次**，之后只有"被阻塞 / 全做完"能开口。
- 载体降级链（5 级：原始数据 → 可执行里的常量 → 专用格式原始结构 → 参考图/视频量化 → 阻塞）→ `reference/asset-sources.md`。
- 写通用设施前先查**引擎能力表**（UI / 池 / 存档·设置 / 图集 / 本地化 / 配表 / 动画 / 相机 / 场景 / 音效）—— 引擎有就用引擎的。
- 细节 → `reference/workflow-and-standards.md`、`reference/engine-mental-model.md`

## §8 硬性约定（判据；全文见全文版）

> 每条**全文（案例 / 代价 / 出处）在 `reference/rules-full.md`**：日志 §7 · 品牌 §1.6 · 配表 §1.7 + `patterns/table.md` · 临时文件 §1.8 · 原版资源 §1.9。

| 主题 | 判据 |
|---|---|
| 日志 | 非预期分支必须留痕（含 `switch` 的 default、高频回调只报一次）；`Game.Logger.Info/Warn/Error(tag,msg)`，⛔ 裸 `Debug.Log` |
| 品牌 | 引擎自称逐字 `clover-engine`；**首页画面底部**必须有 `by clover-engine`（判据 = 实机截图 / 运行时节点树，⛔ 不是 grep 源码） |
| 配表 | 一个功能一个**中文名** xlsx；页签名 = 表名**含后缀**（`skill_cs`）；运行时只走 `Tables.Default.X.Get(id)` |
| 临时文件 | 一次性产物只放 `.ai-tmp/test/`；驱动 `.ai-tmp/drivers/`；宿主 `.ai-tmp/hosts/`；**取证截图 `.ai-tmp/screenshots/`**（⛔ 不进 `Assets/`）；**判据资产**（探针 / 驱动 / 量法脚本 = 删了就不能重新判定同一件事的东西）⛔ 不算一次性 ⇒ 落 `tools/probes/` 并**提交**；⛔ 不许散落到项目根 / `client/_dev/` / `Assets/` / `策划/` / 工程外 |
| 原版资源 | 下载 / 解包素材只放 `<项目根>/原版资源/`（含 `清单.md`）；进工程**只复制被引用的那几个**（⛔ 整包/整表全量搬；闸门 `tools/check-assets.ps1`，骨架 = `reference/check-assets-template.md`，判据 = **五种引用形态**覆盖，⛔ 不是"文件名 -notmatch"），路径收敛到 `Core/ResPaths.cs` |
| 工程卫生 | 一个 `.cs` 一个 MonoBehaviour；`.ps1` 纯 ASCII 或带 BOM；禁 `PlayerPrefs` / 裸 `Input` / `GameObject.Find` / `Instantiate(` |

## §9 路由表（做具体事前，只读这几处）

| 我要… | 读 |
|---|---|
| 建新工程 | `scaffold/new-project.md`、`reference/fast-compile-loop.md` |
| 写业务代码前 | `reference/architecture.md`、`reference/engine-mental-model.md` |
| 菜单 / 流程编排 | `patterns/client/app-flow.md` |
| UI / 外观 | `reference/visual-loop.md`、`patterns/client/ui.md` |
| 3D / 网络 | `patterns/client/3d-mmo-basics.md`、`patterns/client/network.md` |
| 配表 | `patterns/table.md` |
| 改引擎 | `patterns/engine-fix.md`（最小复现 → 最小修复 → **根因写进代码注释 + 回归用例**） |
| 派活 / 编排 | `patterns/multi-agent.md`、`scaffold/agent-impl.md` |
| **"检查过 / 是不是 1:1" 怎么机械判** | **`patterns/full-coverage-audit.md`**（复合产物穷举矩阵：12+3 维度 → 实体清单 → 状态矩阵 → 判据 → 闸门）；实例化模板 **`scaffold/coverage-matrix.md`** |
| 验收 / 交付 | `reference/game-delivery.md`、`reference/verify-template.md`、`reference/design-review.md` |
| 素材 | `reference/asset-sources.md` |
| 服务端 | `patterns/handler.md`、`patterns/datadef.md`、`patterns/signup-login.md`、`patterns/auth-server.md` |
| **掉帧 / 卡 / 帧率异常** | **`experience/perf-triage.md`**（**先证伪环境（渲染设备）→ CPU/GPU 拆分 → A/B → 才轮到代码红旗**） |
| 踩坑 / 时间黑洞 | `experience/README.md`、`experience/time-sinks.md`、`experience/verify-recipes.md` |
| Unity CLI / Pipeline | `reference/unity-cli.md`、`reference/pipeline-and-unity-cli.md` |
| 服务端环境 | `reference/server-env.md` |
| **规则全文版**（案例 / 代价 / 出处） | `reference/rules-full.md`（文末「条目索引」） |

## §10 收尾必念（与 §0 同源；防中段遗忘）

1. **1:1 复刻 A**：A 有 ⇒ 做，A 没有 ⇒ 不加，不许发挥。
2. **做完才交付**：没做完只说"还没做完"；⛔ 不同一条里既说完成又列未验、⛔ 不问"要不要继续"。
3. **取证要批量（不设次数上限）**：进 Play 记账、能离线判的不进 Play、驱动先复用；
   ⛔ **"次数 / 预算 / 轮次 / 上下文"都不是停工理由 —— 验不完就继续验**（§0.1 ①）。
4. **采集即冻结**：采完又改代码 ⇒ 证据作废，只重采受影响的行。
5. **有 FAIL 就不许说"完成"**；合法结束只有"全做完"或"真被阻塞"。
6. **"卡 / 掉帧"先证伪环境**：`SystemInfo.graphicsDeviceName` 不是真 GPU（= 软件渲染）⇒ 停，修环境，⛔ 别去查代码。
7. **画面 / UI / 表现必须 1:1 原版**：唯一判据 = 与 A 原版画面同机位并排比；**任一 `不一致` ⇒ 不可交付**；
   ⛔ 数字全对但画面不像 = **没做完**（§0.1 ②）。
